%h1 Assign leaves
%td= select_tag 'user', options_from_collection_for_select(current_organization.users.all.entries, :id, :email), :id => "cmbuser", :prompt => "Select user"
%div{:id => "assign_leaves"}