require 'spec_helper'

describe DashboardController do
  
  describe "GET #index" do
    it "should be able to access only if subdomain exists"
  end

  it "After Confirmation of Admin should be redirected to dashboard index"
  it "After login it should be redirected to dashboard index"
  it "Login screen url should have a subdomain for that organization" 
  end
end
